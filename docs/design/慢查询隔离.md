# 慢查询隔离

# 慢查询隔离

# 接口

/config/slow_search_isolation

```plaintext
curl -XPOST -H "content-type: application/json" -d'
{
  "slow_search_isolation_enabled": true
}
' http://${VEARCH_URL}/config/slow_search_isolation
 
配置参数保存在etcd中。可动态配置
```


| 字段名称 | 类型 | 描述 |
|---|---|---|
| `slow_search_isolation_enabled` | `bool` | 指示是否启用了慢查询隔离功能。 |


# 慢查询

```plaintext
func parseSlowSearch(indexParams *entity.IndexParams, indexType string, req *vearchpb.SearchRequest) {
    if req.TopN >= 500 {
        req.IsSlowSearch = true
        return
    }

    if indexParams != nil {
        if (indexType == "IVFFLAT" || indexType == "IVFPQ") && indexParams.Nprobe >= indexParams.Ncentroids/10 {
            req.IsSlowSearch = true
            return
        }
    }

    if len(req.RangeFilters)+len(req.TermFilters) >= 3 {
        req.IsSlowSearch = true
    }
}
```

目前以上三种查询语句会被识别为慢查询：

1.TopN&gt;=500, 指定期望返回的结果个数超过500

2.Nprobe &gt;= indexParams.Ncentroids/10 适用于ivfflat和ivfpq索引，检索时指定的nprobe超过聚类中心个数的十分之一，目前只限制了cpu ivf索引

3.标量过滤条件个数大于3

识别为慢查询的请求标记IsSlowSearch为true

# 慢查询隔离

```plaintext
ps service启动时创建慢查询队列，读写队列      
//慢查询隔离，读写队列分离，慢查询队列设置为读队列大小的1/4
s.read_request_concurrent = make(chan bool, s.concurrentNum)
s.write_request_concurrent = make(chan bool, s.concurrentNum)
s.slow_search_concurrent = make(chan bool, s.concurrentNum/4)


//ps节点根据IsSlowSearch标识和method识别请求为慢查询，一般查询，其他操作
//慢查询请求放入slow_search_concurrent等待队列
//一般查询放入read_request_concurrent读队列
//其他操作放入write_request_concurrent写队列
var concurrentCh chan bool
if req.SearchRequest != nil && req.SearchRequest.IsSlowSearch {
    concurrentCh = handler.server.slow_search_concurrent
} else if method == client.QueryHandler || method == client.SearchHandler ||
    method == client.GetDocsHandler || method == client.GetDocsByPartitionHandler ||
    method == client.GetNextDocsByPartitionHandler {
    concurrentCh = handler.server.read_request_concurrent
} else {
    concurrentCh = handler.server.write_request_concurrent
}
```

